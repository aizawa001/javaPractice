package model;

public class RegisterUserLogic {
	public User execute (User user, String jdbcUrl, String dbUser, String dbPass) {
		if (user.getName().matches(".{3,}") && user.getPass().matches(".{3,}")) {
			return user;
		}

		return null;
	}
}
